# Vrdct standing board

> **Honest scope — devnet bonds are not real capital.** A devnet dispute is reputation, not an
> economic incentive.
>
> What is real on devnet is the claim, its source descriptor, deterministic re-execution, and that
> anyone can run the exact `vrdct check` command carried by each row. An uncontested row is
> **uncontested, not proven**. This board has no sound (market-hours-guarded) row yet: identifying
> one requires an indexer-grade historical RPC, not a guess from a rate-limited public endpoint.

This is a committed record generated by `keeper/vrdct-keeper.mjs`, not a landing page. Its dated
files and this index are written from chain time and are diffable in git. A row is published only
when the keeper can re-fetch its CMLS descriptor and reproduce the market's `inputs_hash`; the row
then supplies its own direct falsifier command including its cluster `RPC` and source `SOURCE_RPC`.

## Current record

No devnet position has been opened from this repository. On 2026-08-07 the configured Vrdct program
ID was also verified absent from devnet, so there is no truthful Market address for a committed row
or a `vrdct check` command. This zero-row record is honest about that absence; it does not claim
that any venue is sound or uncontested. The committed local E2E in
[`keeper/tests/standing-board.local.mjs`](../keeper/tests/standing-board.local.mjs) proves the row
format and verifies every required field against a real local Market account.

The measured named RED candidate remains Jupiter Lend's SPYx account
`A2GDb4Um4Tr42iKgPz5fQ2d7pYTnaUuHN3d5V41Cywff`; its source measurement and wording are recorded
in [`docs/tasks/005-subject-set.md`](../docs/tasks/005-subject-set.md). It is deliberately **not** a
board row until a devnet Market binds that source and supplies the required check command.
