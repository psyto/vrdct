{
  "name": "vrdct-onchain",
  "version": "0.0.1",
  "description": "Vrdct on-chain bond & settlement — re-execution decides the payout.",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "anchor build && anchor build --no-idl -- --arch v3",
    "test:unit": "cargo test -p vrdct-bond",
    "test:integration": "cargo build-sbf --manifest-path programs/vrdct-bond/Cargo.toml --arch v0 --sbf-out-dir target/program-test-deploy && BPF_OUT_DIR=$PWD/target/program-test-deploy cargo test -p vrdct-bond --test state_machine -- --ignored",
    "bond": "node client/bond-live.mjs"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  }
}
