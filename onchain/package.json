{
  "name": "vrdct-onchain",
  "version": "0.0.1",
  "description": "Vrdct on-chain bond & settlement — re-execution decides the payout.",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "anchor build && anchor build --no-idl -- --arch v3",
    "test:unit": "cargo test -p vrdct-bond",
    "bond": "node client/bond-live.mjs"
  },
  "dependencies": {
    "@solana/web3.js": "^1.98.4"
  }
}
