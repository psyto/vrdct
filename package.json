{
  "name": "vrdct",
  "version": "0.0.1",
  "description": "The neutral resolver — re-execution decides the payout. Verifiable claims resolve on-chain-state prediction markets and agent-payment escrow.",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "demo": "node demo.mjs",
    "generate:parity-vectors": "node tests/generate-parity-vectors.mjs --write",
    "test:canonical": "node --test tests/canonical-inputs.test.mjs && node tests/generate-parity-vectors.mjs --check && node tests/generate-definition-vectors.mjs && (cd onchain && cargo test -p vrdct-bond)"
  },
  "keywords": ["re-execution", "resolver", "prediction-markets", "solana", "verifiable-claims", "oracle-free"]
}
